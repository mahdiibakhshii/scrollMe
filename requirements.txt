# Validated on the server's Python 3.14 (prebuilt wheels, no compile).
# socketio/engineio pinned to the 4.x / 3.x line on purpose: they speak
# Engine.IO v3, which is what the phone client's socket.io 2.3.0 JS expects.
aiohttp>=3.10,<4
python-socketio>=4.6,<5
python-engineio>=3.14,<4
python-osc>=1.8
# Local testing helpers only (td_emulator.py / test_simulation.py):
requests>=2.31
websocket-client>=1.8
